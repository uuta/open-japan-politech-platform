{
  "name": "@ojpp/ingestion",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "ingest:finance": "tsx src/political-finance/ingest.ts",
    "ingest:parliament": "tsx src/kokkai/ingest-sessions.ts && tsx src/kokkai/ingest-bills.ts",
    "ingest:manifesto": "tsx src/manifesto/seed-policies.ts",
    "ingest:all": "pnpm ingest:finance && pnpm ingest:parliament && pnpm ingest:manifesto",
    "test": "vitest run"
  },
  "dependencies": {
    "@ojpp/db": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
